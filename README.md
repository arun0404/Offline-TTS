# Offline-TTS
Offline Text-to-Speech (TTS) web app built using HTML, CSS, and JavaScript. Uses the browser’s Speech Synthesis engine to generate speech locally with voice selection, preview, and download support.
